---------------------------------------------------------------------------------------
  Track Inspector 2 - Changelog     
---------------------------------------------------------------------------------------

* v2.4.47 (2026-08-04)
  # ask for name when saving tag presets via actions
  # force visibility refresh when loading tag presets
  # fix crash changing use special tags in some cases

* v2.4.46 (2026-07-30)
  # fix: addon settings per project: fix problem not parsing new settings when save settings per project
  # maintenance updates for addons 

* v2.4.45 (2026-07-19)
  + Support Tooltips in add-ons panels

* v2.4.44 (2026-07-04)
  # small fix in middle button drag to scroll panels

* v2.4.43 (2026-06-29)
  + support for Tracks addon v3.11+ (save settings per project)
  # avoid project set dirty when creating hidden postfader sends

* v2.4.42 (2026-06-13)
  + Receive Presets: Receives panel menu: Show Receive Presets Buttons
  + Receive Presets: Show track names on buttons
  + Receive Presets: Use Pre-Fader (Create receives from presets as pre-fader) 
  + Receive Presets: Use Side-chain (otherwise SHIFT click to create receive as sidechain)
  # changes for Tracks addon v3.10+
  # bug fixes in sends autofind

* v2.4.41 (2026-05-12)
  # fix Do not collapse panel option on Tracks panel

* v2.4.40 (2026-05-10)
  # updates for addons instance configurations
  # compatibility with Tracks v3.9 
  # fix do not collapse option on panel header click
  # Track native Notes support for 7.7dev versions.

* v2.4.39 (2026-04-22)
  # fix: scroll track into view when using show envelopes of selected track

* v2.4.38 (2026-04-02)
  # fix: improve +track send search
  # fix native track notes support for dev version only for now

* v2.4.37 (2026-02-01)
  + Support for native Track notes in Notepad panel. 7.60dev+ (+note button)  (+text button is now the old TI text notes)
  # improvement in addon buttons
  # going back to previous track after removing tracks is now optional. it was causing problems. "gobackafterremovetracks" variable.

* v2.4.36 (2025-12-13)
  # fix opacity mouse over toogle buttons in settings
  # fix reference line width
  # fix dragging panels issue with tags panel
  # fix issues in fxchain panel with some font sizes

* v2.4.35 (2025-12-06)
  # improvements in some addon buttons
  # fix issue in "autotag using folder names"

* v2.4.34 (2025-11-21)
  # fix strange crash
  # notify if it cannot open file for output when creating tag actions

* v2.4.33 (2025-11-18)
  # fix mouse wheel over pages scroll panels 
  # improvements middle mouse button drag up and down to scroll panels. fix with Tracks and Tags panels
  # fix addon refresh in some cases

* v2.4.32 (2025-10-14)
  + Meter: 1/2 button to use stereo 1/2 channels only for send to aux meter track (global setting for now)
  # stabiliy fix Track Versions

* v2.4.31 (2025-10-13)
  # Fix show buttons in FX Chain panel
  + Meters panel menu: Show Peaks Hold Max
  + Meters panel menu: Show SL Max
  # fix midi send destination channel number display
  # fix tags not updating MCP visibility in linked TCP MCP visibility
  # fix tags isolating TCP in linked TCP MCP visibility
  # fix scroll into view after isolating tag visibility
  # isolate button only affect TCP if MCP not linked to TCP
  # fix zoom to fit after isolating (more or less)
  # fix macos tag list buttons alignment?

* v2.4.30 (2025-10-08)
  # fix crash if removing all tracks
  # some adjustments in some areas of the meter.  master MONO button relocated. margins...
  # fix when removing "Tag Color to Use"
  # improvements in setting "Tag Color to Use": assign to multiple selected tracks. use same 
  left click button to set and to remove.

* v2.4.29 (2025-09-29)
  # fix master instance and locked tracks. thanks @kotsaris [p=2894206]

* v2.4.28 (2025-09-28)
  # fix in track history

* v2.4.27 (2025-09-27)
  # Go Back button scrolls into view new selected track
  # Go to next track after deleting a track
  + Tags Panel menu: Autotag untagged tracks. If tag is untagged, tag it with parents autotags

* v2.4.26 (2025-09-18)
  # fix scrolling issue in Tracks when using Tracks and Tags panel on same page.
  # fix tag scrolling start
  + draw rectangle highlighting track tags on tag list

* v2.4.25 (2025-09-15)
  # fix optimization on tag mcp isolation

* v2.4.24 (2025-09-12)
  # fix fixes on a better day
  
* v2.4.23 (2025-09-11)
  # the final bug fix of the year. 

* v2.4.22 (2025-09-11)
  # stability fix (the real one) track change refresh not working when undo track is disabled in undo history. 

* v2.4.21 (2025-09-10)
  # stability bug fixes
  # fix middle drag horizontally in tags list not working at first if pagination is set to 0
  # fix macos detection (broken recently)

* v2.4.20 (2025-09-05)
  + Track navigation history: < button to go back previous track. Right click to go forward
  + Settings: Show Go Back Button. Show/Hide the button to go back in track history
  + List special tags button (to hide or show the list)
  + Project's tag list maximum number of tags. scrollable with mouse wheel.  Rmb to disable paging.
  + Project's tag list filter search button. Rmb to clear filter.
  + Middle mouse button drag horizontally to scroll tags columns
  # adjustments in track tags display, more margin and area border
  + Set font number for track's tags
  # adjustments in tags presets display
  # tag toggle all do not affect always off tags
  + Settings: Footer message when detect new version updated.
  # footer notifications adjustments, progress bar. configurable time for each notification. settings Footer Notification( set 0 to disable footer notification)
  # fixes in ItemsTS special tag, when stopped
  # optimizations

* v2.4.19 (2025-08-15)
  # fix: bug when creating actions from TI. Needs to recreate Tag actions, FX group actions, Sends actions.
  # fix: support state on/off for tag isolate actions
  # fix: refresh addons after tag visibility actions
  # stability fixes

* v2.4.18 (2025-08-07)
  + Tags Panel menu: Show Auto Refresh Enable button (for manual visibility refresh)
  + Tags Auto refresh button to enable/disable auto refresh
  + Tags Auto Refresh action: HeDa_TI_Tags Autorefresh
  + Tags Manual refresh button
  + Tags Manual Refresh action: HeDa_TI_Tags Refresh
  # fix refresh add-ons when using external actions
  # theme: Button opacity changed to button color Factor
  # fix color button and button borders on add-ons buttons
  # fix crash setting custombutton color sometimes

* v2.4.17 (2025-07-28)
  + Tags: Resizable tag name column. (drag from the column header separator) (right click to auto fit)
  # Polished tags list, more compact. fix vertical alignment of some tag buttons
  # Some Tag buttons changed to image buttons (avoid font problems with the special characters)
  + Theme settings: Button border on hover.
  # fix Autotag always tagging children without asking
  # more small bug fixes

* v2.4.16 (2025-07-20)
  # fix: Maintain tags when Rendering to new version.
  # fix. Clear versions (Reset to V1) when deleting versions and only one left
  # improved footer message popups

* v2.4.15 (2025-07-17)
  # fixes when no archived suffix is set
  # fix save tags data after merging tags from file

* v2.4.14 (2025-07-12)
  # fix page buttons size (Pages extra size theme setting)
  # fix page button text to be centered
  # fix in external refresh track action, to include refresh of add-ons.

* v2.4.13 (2025-07-07)
  # fix refreshing special tags when changing color for special tag
  # fix inactive versions to always off by default

* v2.4.12 (2025-07-03)
  # fix '<bad string>' where the Change track VU mode button. Thanks @inthevoid [p=2867871]
  # fix and improvements for add-ons on change track selection by selecting media items, and marquee media items
  # some changes for next version of Tracks Addon

* v2.4.11 (2025-06-15)
  + Settings: Inactive version tag. Use this tag for the inactive versions. 
  # fix: refresh color by tag when switching track versions

* v2.4.10 (2025-06-14)
  # fix bug related to storing Tag to use for Color in 2.4.9

* v2.4.9 (2025-06-13)
  # only ask once to autotag children when tagging multiple folders at the same time
  # fix SHIFT+click to special tags to toggle all on/off
  # update tracks panel when renaming tracks from TI
  + Save Tag to use for Color inside Track EXT data.
  # fix: TI send recreated when switching pages. thanks @BogdanS [p=2869450]
  + Tag Presets menu. Set font number to use for presets buttons
  # fix error when archiving add-on was not enabled and changes for latest version
  # fix multichannel send to aux after track change
  # fix Archive tag. Now it is normal tag, not special (Archived) tag

* v2.4.8 (2025-05-26)
  # fix: crash purging unused track versions when there are no track versions
  # fix: refresh visibility of tracks by tag when importing track templates
  # fix: Undo blocks on track versions actions

* v2.4.7 (2025-05-21)
  + Tags Panel menu: Delete All Unused Tags from List

* v2.4.6 (2025-05-20)
  + (RecArm) special tag for record armed tracks.
  # fix focus arrange (broken in recent REAPER version)
  # fix: automatically purge track versions that do not exist in project.
  # internal update for add-ons

* v2.4.5 (2025-05-06)
  + Reorder tag preset (tag preset right click menu)
  # show unused tags by default
  # show unused tags after merging tags file
  # fix in tag name column width to fit largest tag name
  # fix in track version go to next and previous versions buttons < >
  # support multiple tracks for next and previous versions buttons < >
  # fixes in rendering new version
  # fix settings button
  # fix issue big number displayed in channel mode button with sends with audio channel set to None

* v2.4.4 (2025-04-27)
  + Freeze panel: Unfreeze to New version
  + Setting: unfrozen suffix for unfreeze to new versions.
  + Tag tracks with "Frozen" tag when freezing tracks manually or with TI freeze buttons.
  + Setting: "Frozen" tag name.
  + Tag tracks with "Rendered" tag for new version rendered tracks.
  + Setting: "Rendered" tag name.
  + TagPresets Menu: Option to set the number of Tag Presets per project
  + TagPresets Menu: Option to show One tag preset buttons per line
  + Tags: button to show/hide unused tags in the tag list
  # Tag buttons width fit to entire column.
  + Get Tag properties from Default.TItags if tag does not exist when importing tracks templates.
  + ItemsTS also works now without time selection as a zone from play cursor to x seconds after.
  + Setting: ItemsTS time after playcursor when no time selection 
  # fixes and optimizations in Special Tags
  # fix tags isolating visibility and untagged tracks
  # fix bug in going to panel
  + Theme Setting: button border opacity

* v2.4.3 (2025-04-23)
  # fix bug in track versions suffixes bug in track names.
  # fix paste tags

* v2.4.2 (2025-04-21)
  + Shift+Rightclick on Tag, to add to selection of tracks with tag
  + "remove_stemsuffix": removes " - stem" suffix in track names when rendering to new versions
  # remove "Not Active version" suffix by default.
  # various bug fixes in tags

* v2.4.1 (2025-04-20)
  + Tag archived tracks with special Archived tag
  # fix isolate tag button on special tags
  # Setting: Autotag from track suffixes
  # fix go to panel and switch pages actions with multiple TI instances running
  # AutoTag button now tags selected tracks only
  # Tags bug fixes when pasting tracks or inserting templates

* v2.4.0 (2025-04-15)
  + Tags code rewrite. better performance, improvements, bug fixes, new tags load and saving format. 
  Old tags format is automatically imported.
  + Tags: automatically tag duplicated tracks.
  + Tags: automatically tag pasted tracks.
  + Tags: automatically tag inserted track templates.
  + Tags: automatically tag new track inside folder with autotag.
  + Tags: Setting to show tag preset names on the buttons instead of tag preset numbers.
  + AutoTag button: also tag the tracks from EXT data. 
  + confirmation prompt when removing tag from project: Remove this Tag from All Tracks?
  + Tag Isolate button column
  # Settings: Save tags in EXT data inside tracks: now enabled by default.
  + Settings: Tag TCP and MCP customization of text in the tag buttons
  + Auto-hide (screen edge docking)
  + Frameless (no titlebar, no border) always on when using auto-hide.
  + Meter: various adjustments in the meters. text of the LUFS-M or LUFS-S at the
    meter level and LUFS-I at the bottom of the meter.
  + Show Max peak hold number, and shade the headroom to 0
  # Changed default meter scale to full scale
  + Meter: show PLR (Peak to Loudness Ratio)
  + Meter: thicker lines in histograms
  + Meter scale minimum dB. made the setting also in settings page. It was only in
    the meters menu.
  # Bug fix sends for meter with multichannel tracks 
  + Right click in Pan Direction to show pan law menu
  + Right click in Pan Fader to show Pan mode menu
  + Set Quantum per project. Automatically sets latency quantum if Project has specific
    quantum set. (to change latency per project with Pipewire jack as audio device)
  + Use new action "HeDa_Set Project Quantum.lua" to set the quantum latency override for a project
  # Track number right click to go to Color panel.
  # fix naming Track versions rendered suffixes
  # optimizations and bug fixes

* v2.3.17 (2025-01-02)
  + Guide PDF, Guide Add-ons PDF
  + Access to guide also from ? button at the bottom
  # removing Previous common Track Inspector actions, renaming them to HeDa_TI_*******
  # fix: actions generated by TI broken recently (fx group actions, tag actions etc...)
  # fix: bug refreshtrack action missing
  # fix: issue with track detection depending on some order of panels

* v2.3.16 (2024-11-14)
  # Freeze: fix setting freeze n for all multiple selected tracks.

* v2.3.15 (2024-08-14)
  # fix focus arrange when opening fx by default. added setting "floatfx_focusarrange"
  # some cleaning in parts of the code

* v2.3.14 (2024-03-24)
  + SpecialTag (FX) Tracks that have FX
  + SpecialTag (FX-PDC) Tracks that have FX and track PDC is > 0

* v2.3.13 (2024-03-20)
  + stereo/multi-channel buttons for sends. 
  + SpecialTag (NoRouting) Tracks without sends/receives
  + SpecialTag (NoFX) Tracks that don't have FX
  + SpecialTag (MultiChannel) Tracks with more than 2 channels

* v2.3.12 (2024-02-15)
  # FX chain panel: FX plugin buttons width adjusted to panel width.
  + Sends panel: Added number of channels for Main send.
  # Sends panel: fix channel offset display
  + Meters panel menu: Show RMS window button.
  + Meters panel menu: Show PRE-FX button.
  + Meters panel menu: Show PFX button.
  + support finding CLAP plugins in frozen plugins list.
  + Tags panel: Added to panel menu: Zoom to fit vertically after tag isolating TCP visibility
  + Tags panel: fix tags presets font size
  # General panel: fixes in hide others (Rmb persistant mode)
  # General panel: fix set number of channels works for multiple selected tracks
  # General panel: fix lock track controls for multiple selected tracks
  # some global optimizations and small bug fixes

* v2.3.11 (2024-02-05)
  # fix in panning histogram and correlation meter with retina scale.
  + Settings: added correlation meter height setting

* v2.3.10 (2023-11-24)
  # fix crash in some configurations when not using automation mode. Thanks @Etalon [p=2736181]
  
* v2.3.9 (2023-11-22)
  + Sends panel menu: add option to show all send presets buttons with track name and/or track number

* v2.3.8 (2023-09-07)
  # fix: make sure extensions are saved when saving settings
  # fix: broken freeze

* v2.3.7 (2023-08-26)
  # fix lock button. thanks @hoangmytr [p=2705759]

* v2.3.6 (2023-08-16)
  + FX chain panel: Added PDC button to show plugin's PDC number
  + METER panel menu: Show peak mode button
  + METER panel menu: Show panning
  # bottom-left Settings button as image png instead of font character
  # fix broken font factor change with Lmb+CTRL+mouse wheel

* v2.3.5 (2023-08-15)
  + 1.50x theme images (in Default_6.0 theme)
  # fixed too large 2.00x freeze icons
  # fixed some window size floating issues
  # more fixes for different font sizes

* v2.3.4 (2023-08-09)
  + REAPER 7 support
  # fix: broken in 2.3.3 scroll with FX chain panel

* v2.3.3 (2023-08-06)
  # fix: clicking on FX plugin sometimes was changing order depending on font scale. Thanks @inthevoid [p=2697867]

* v2.3.2 (2023-08-04)
  + Retina scaling support
  + Additional factor scaling. CTRL+left click + mouse wheel, or in settings theme
  + CPU Saver (save energy after some time has passed without activity)
  + Freeze: Ctrl click to select multiple Tracks to freeze in the list
  + Freeze: Right click the "x tracks" button to do the most used one(Select tracks to freeze with MIDI items)
  # Freeze: different color for selected tracks in Tracks to Freeze list
  # Freeze: fix: refresh Tracks to Freeze list when changing Freeze mode of some tracks
  # Freeze: fix in FTO and general panel buttons position
  # fix: master track instance to not check current selected track 
  # fix: selecting master track in normal instance
  # Simple meters renamed to "Use REAPER Meters"
  # fix overlap slider with 100W text in panning
  # fix rounding in dbscale numbers
  # fix knobs size in FX Chain panel
  # improved toggle buttons in settings page
  
* v2.3.1 (2023-07-19)
  # fix go back button from right click go to send
  + Settings: Show Track ID number
 
* v2.3.0 (2023-07-17)
  + Freeze: When selecting tracks to freeze, shows a list of the tracks
  + Freeze: Button to add dummy plugin workaround(**Freeze tracks only**), as seen in Reapermania https://www.youtube.com/watch?v=EZyrei3vBiQ
  + Freeze: General Settings for **Freeze tracks only** name, and FTO button
  # changes in top bar with track color, track id, track name, and track icon.
  + Font 5 and Font 5 size. Used for the Track name
  + Setting "Draw automation mode color"
  + Setting "Label of Panning when centered". it was "CENTER" before. now it is "C" by default. you could also set it to nothing.
  # fix color of selected track affecting master track instance
  # bug fixes

* v2.2.26 (2023-05-27)
  # fix empty TI when no selected tracks (broken in 2.2.25)

* v2.2.25 (2023-05-04)
  # fix duplicate suffix in window name when entering settings
  # fix do not force set include selection track in undo settings
  # fix vumode menu in simple meters mode
  # fix in how meters are displayed in some vumodes
  # small bug fixes

* v2.2.24 (2023-04-09)
  # fix instrument prefix ·
  # added to remove CLAP, CLAPi, LV2, and LV2i prefixes from plugin names.
  # small bug fixes

* v2.2.23 (2023-04-01)
  + Setting: Do not tag hidden tracks
  # use reaper.SetTrackUI functions when changing Fader and Panning (REAPER 6.71+)
  # Show archived tracks button: Left click unhides archived tracks leaving the rest unchanged. Right click as previous behaviour showing only the archived tracks.

* v2.2.22 (2023-03-28)
  # optimization in internal function. less CPU usage
  + Settings: Automatically tag children tracks by default
  + Settings: Enable prompt to tag children tracks
  # fix: keep page panel scroll position in addons

* v2.2.21 (2023-02-26)
  # fix add autostart entry in new line

* v2.2.20 (2023-02-26)
  # fix autostart button for Master instance. Thanks @Hipox [p=2652355]

* v2.2.19 (2023-02-20)
  # fix: Autoenlarge track when using per track option, not working because the height was not saved for each track
  + Settings: TI window/tab name suffix (useful to have different tab names for each instance) It needs restart TI to reflect change

* v2.2.18 (2023-02-17)
  # fix: when closing project sometimes, crash bad argument #1 to 'CSurf_TrackToID'

* v2.2.17 (2023-02-14)
  + General panel: track channels
  + Settings: Track Channels suffix setting: sets the track channels button suffix after the number. "ch" by default
  
* v2.2.16 (2023-01-15)
  # fix: mute buttons on hardware sends

* v2.2.15 (2023-01-08)
  # fix crash when starting with no tracks selected (broken in 2.2.14)

* v2.2.14 (2023-01-07)
  # fix: MIDI latch-replace. Thanks @inthevoid [p=2631209]
  + Add support for multichannel send to aux track for Loudness Meter surround.

* v2.2.13 (2022-12-26)
  + FX Group actions: HeDa_Track Inspector 2 VIP_Bypass FX Group x
  + FX Group actions: HeDa_Track Inspector 2 VIP_UnBypass FX Group x
  + FX Group actions: HeDa_Track Inspector 2 VIP_Offline FX Group x
  + FX Group actions: HeDa_Track Inspector 2 VIP_Online FX Group x
  + FX Group actions: HeDa_Track Inspector 2 VIP_SetFXGroup All Tracks.lua
  + FX Group actions: HeDa_Track Inspector 2 VIP_SetFXGroup Current Track.lua
  + FX Group actions: HeDa_Track Inspector 2 VIP_SetFXGroup Selected Tracks.lua

* v2.2.12 (2022-11-26)
  # added settings to configure each plugin name in case the meters are not working again(6.70+):
      "dpMeter_pluginname": dpMeter plugin name to use in tracks. Configure parameters in Global options.ini file
      "ti_pluginname": name of Track Inspector JS plugin to use in tracks
      "cockos_pluginname": name of Cockos Loudness JS plugin to use in tracks
    and for the monitor FX Chain
      "monitorfx_dpmeter_name": Name of the dpMeter instance in the Monitor FX Chain
      "monitorfx_ti_pluginname": Name of Track Inspector JS plugin to use in Monitor FX chain
      "monitorfx_cockos_pluginname": Name of Cockos Loudness JS plugin to use in Monitor FX chain
  + added dpMeter5nogpu support and parameters in Global Options.ini

* v2.2.11 (2022-11-04)
  # Fix large freeze button images in Default_6.0 - 200 theme.
  # fix send and receive bars position overlapping some buttons in some font configurations

* v2.2.10 (2022-10-28)
  # Fix Load Settings... in some configurations
  # fix meter grid db text with retina configurations
  + Enlarge track height per track
  + initial language translations support (testing)

* v2.2.9 (2022-08-12)
  + Added different languages support for actions detection. TI reacts to these actions.
    default english file: language_default_en.lang
    added a spanish file that works with Espanol_JavierRobledo_v2.ReaperLangPack: language_es.lang
    you can create your own lang files for other languages and set it in TI settings to use that file.

* v2.2.8 (2022-08-07)
  + theme: Knob size setting (to control wet/dry knobs size independently of the font size)

* v2.2.7 (2022-05-30)
  # fix: Simple meters left/right channels swapped in 2 channel tracks and peaks mode.
 
* v2.2.6 (2022-05-26)
  # Different custom buttons for old TrackVersions and addon Versions panels

* v2.2.5 (2022-02-23)
  # fix focus issue opening fx plugins. Thanks @Skorobagatko

* v2.2.4 (2022-02-16)
  # fix: crash when starting TI master instance in some cases when different configuration of addons in main instance..

* v2.2.3 (2022-01-24)
  # fix focus on notepad text edit. Thanks @mozart999uk
  # fix do not autotag when showing archived tracks. Thanks @HighVoltage

* v2.2.2 (2022-01-22)
  # NOTEPAD: fix +text button to add/edit text notes using the new note item editor modal window
  + NOTEPAD: Load/Save Track notepad data in "P_EXT:notes" which is saved in track templates.
  + NOTEPAD: Load/Save Track notepad data in "P_EXT:images" which is saved in track templates.
  # SENDS: fix sends name wrap to new line if panel is too narrow
  # fix crash: make sure TPMAX_OVER is a number, not a string before saving it to settings.  Thanks @Reflected [p=2517156]
  # fix do not set auto online FX by default

* v2.2.1 (2021-12-20)
  # fix rendering issue with cursor offset

* v2.2.0 (2021-12-14)
  + Playback offset: Cursor offset button: move playback cursor before playing with maximum negative offset in project
  + Playback offset: option: display maximum negative offset in project
  + Playback offset: option: display all tracks with playback offsets active
  # fix RMS1/2 vumode meter

* v2.1.4 (2021-12-05)
  + Support for Addon actions
  # fix Loudness Range (LRA) when using Cockos loudness meter mode. Thanks @Gianfini
  # fix crash right clicking empty page

* v2.1.3 (2021-11-22)
  # remove auto inserting plugins in the Monitor FX Chain if not found.
  # provide Cockos Fx chain in the FX Chain folder (to be manually added in monitor fx when in Cockos loudness meter mode)
  + focus element for add-ons Go to Panel actions 

* v2.1.2 (2021-11-05)
  + Add plugins to Monitor FX Chain automatically if not found.
  + Enable/Disable add-ons on different TI instances
  # support for refreshing add-ons when used in multiple instances
  # fix panel header text margins (Thanks @todoublez)
  # fix hidden panels in TI1 not showing in TI2 (Thanks @vsthem)
  # use custom names for normal panels in the page menu
  # Hide plugin prefixes and brand in Freeze info (Thanks @Skorobagatko)

* v2.1.1 (2021-10-30)
  + REAPER 6.40 support for LUFS meters using first two channels only (in simple meters mode)
  # small bug fixes

* v2.1.0 (2021-10-23)
  + Meters Panel: Option: Use Cockos Loudness Meter plugin (instead of dpMeter)
  # fix: restore collapsed state of panels from other pages correctly

* v2.0.3 (2021-10-17)
  + Settings: Auto-Start button: setup TI to start when REAPER starts
  + Sends: +track button fix spacings in list of tracks. Thanks @ericzang
  # some small bug fixings

* v2.0.2 (2021-10-16)
  # fix refresh special tags when enabling/disabling a special tag
  + OnChange event support for Add-ons
  # Add-ons API fix in toggle buttons

* v2.0.1 (2021-10-05)
  # fix General panel menu. Thanks @ericzang
  # fix Sends panel menu. Thanks @ericzang

* v2.0.0 (2021-10-05)
  + Track Inspector Pages
  + Track inspector Add-ons
  + Draggable panels to reorder
  + Improved Settings page and search settings textbox
  + More Theme options, animated mouse hover panels effect
  + API for developement of add-ons
  + dpMeter 5 support
  + REAPER 6.37+ support for wet knobs
  + added support for track VU meter modes in simple meters mode. REAPER v6.34+
  + Freeze line (freezes up to a determined number of plugins only. it is saved for each track)
  + Special tag (Offline) tracks with all plugins offline
  + Receives panel menu: Select all receives command
  + Receives: clickable buttons to set by value. Multiple selected support
  + Sends: Multiple selected sends support for set by value
  + Sends: +preset button displays menu to create sends to presets
  + Setting: dpMeter version to use: to change easier between dpMeter4 and dpMeter5 version
  # Autotag also updates VCA follower for folders having a Virtual Instrument
  # bypass TI monitorfx plugins in Simple meters mode.
  + Sends panel: Parent/Master send toggle 
  + Sends panel: Parent/Master send channel offset
  + Sends panel: +Send search fix first track not searchable
  + Settings: theme: Margin between Pages and Track name
  + Zoom vertical selected tracks and Hide others (left click toggle, right click update on track selection change)
  + Hide others mode: option to show sends/receives/parents tracks of the selected track
  + Settings: theme: Panel background (hover) color
  + Settings: theme: Panel background (hover) opacity
  + Settings: theme: Panel header background (hover) color
  + Settings: theme: Panel header background (hover) opacity
  + Settings: theme: Panel header text (hover) color
  + Settings: theme: Panel mouse hover transition speed
  + Settings: theme: hover effect speed from 0 to 1 (0 to disable, 1 for instant transition)
  + Settings: Theme Margin inside panel Left, right, top and bottom
  + action Switch to next & previous page
  + Settings theme Track name size
  + Settings: option to center fx name
  + Settings: Save/Load full configurations
  + Settings: Panel header alignment: left, center, right
  + Settings: Panel separation line color
  + Settings: Panel background color
  + Panels menu button visible on mouse over
  + Panels dragging zone visible on mouse over
  + Setting: Zoom to fit vertically after TCP isolating tag visibility
  + CTRL + Rmb on panel (anywhere inside panel, not only header)
  + CTRL + Rmb on panel displays a menu with options
  + CTRL + Rmb on panel: Hide panel header
  + CTRL + Rmb on panel: TI Settings and TI dock/undock
  + Settings: Panel header text color
  + Settings: Show Page tabs buttons
  + Settings: Show Track Name, icon, color
  + Settings: Show Bottom
  + CTRL+Rmb on panel name to rename the panel
  # performance and bug fixes
  