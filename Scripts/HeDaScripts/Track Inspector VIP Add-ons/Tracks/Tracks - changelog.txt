---------------------------------------------------------------------------------------
 Changelog (What's new)     
---------------------------------------------------------------------------------------

* v3.15.1 (2026-08-04)
  + Display Settings: Color: Track Name for Frozen Tracks...
  + action: HeDa_TI_Tracks_Save Selected Tracks as Set.lua
  + action: HeDa_TI_Tracks_Save Selected Tracks as Set (ask name).lua
  + action: HeDa_TI_Tracks_Switch to Set n.lua
  # Save as new Set automatically names as "Set-n" by default
  + Set buttons: Shift+Lmb: to rename Set. added tooltips.
  + Volume/pan toolbar button menu: Double click to Reset.
  # fix do not show volume/pan knobs on archived tracks
  + FX buttons: some improvements and Added Alt+Lmb to Remove FX chain.
  # fix switch to other sets when set is pinned
  # fix switch to other sets when set is isolated
  # fixes in arm rec buttons

* v3.15.0 (2026-07-30)
  + Pan Knobs
  + Pan Knob right click menu: options for panning, pan mode,and pan law
  + Volume Knob right click menu: Reset Volume and Pan
  + Volume Knob right click menu: Change Volume by +1 +2 +3  -1 -2 -3
  + Volume/pan toolbar button menu: Pan value/knob/color options
  + Volume/pan toolbar button menu: Show Values on Mouse Action when displaying knob only
  + Volume/pan toolbar button menu: Pan Knob Drag Sensitivity
  + Volume/pan toolbar button menu: Mouse Wheel to cycle knob display options
  # fix track icons when changing active project
  + Main menu: Display Settings: Font Selected Track Names
  # some defaults settings tweaked, fonts for tracknames changed to Inter-Regular, and Inter-Medium

* v3.14.0 (2026-07-23)
  + Volume Knobs
  + Volume/pan toolbar button menu: "Draw Volume Value"
  + Volume/pan toolbar button menu: "Volume Value Color Pos/Neg"
  + Volume/pan toolbar button menu: "Volume Color..."
	+	Volume/pan toolbar button menu: "Draw Volume Knob"
	+	Volume/pan toolbar button menu: "Volume Knob Color Pos/Neg"
	+	Volume/pan toolbar button menu: "Volume Knob Color..."
	+	Volume/pan toolbar button menu: "Prevent Change on Folders"
	+	Volume/pan toolbar button menu: "Knob Drag Sensitivity..."
	+	Volume/pan toolbar button menu: "Mouse Wheel Step for Volume..."
	+	Volume/pan toolbar button menu: "Mouse Wheel Step for Volume (while holding Right mouse button)"
  # Track Name: Abbreviate: abbreviate more if needed by using first letter of each word
  # Track name: name does not fit: Mouse over "..." to show full track name tooltip
  # bug fixes in tags, added track name in tag tooltip box, fix font size
  # bug fixes in the Tracks panel header menu
  # bug fix keep panel height constant when top pinned sets area change

* v3.13.1 (2026-07-20)
  # optimizations in meters
  + Meters toolbar menu: Draw Meters in New Row
  + Meters toolbar menu: Meter dB Scale (displayed when using Draw Meters in new row)
  + Meters toolbar menu: Reference Lines (displayed when playing)
  + Meters toolbar menu: Reference Line A...(dB)
  + Meters toolbar menu: Reference Line B...(db)
  # bug fixes

* v3.13.0 (2026-07-19)
  + Tooltips
  + Isolate Connected button: Isolates tracks and its connected tracks (parents and sends)
  + Isolate Connected button Right click menu: Use Selected Tracks
  + Isolate Connected button Right click menu: Mute Others option
  + Isolate Connected button Right click menu: Show connected
  + Isolate Connected button Right click menu: Show connected in MCP only
  + Added action: HeDa_TI_Tracks_Isolate Connected.lua
  + Toolbar Track name button menu: Abbreviate (abbreviate track names if it doesn't fit)
  + Filter Has Items button: right click menu: Always Show Folders
  + Filter Has Signal button: right click menu: Always Show Folders
  + Always Show folder tracks when diplaying Archived Tracks only
  + CTRL+ALT exclusive solo: while holding CTRL+ALT, move mouse to exclusive solo on hover
  # small bug fixes

* v3.12.0 (2026-07-07)
  # Isolate button: left click + Shift or Ctrl key to act as isolate selected tracks too.
  + Pin button: right click to pin seelected track to TCP pinned area
  + Pin button: left click + Shift or Ctrl key to pin seelected track to TCP pinned area
  + Added action: HeDa_TI_Tracks_Isolate Set.lua
  + Added action: HeDa_TI_Tracks_Pin Set.lua
  + Added action: HeDa_TI_Tracks_Focus Change.lua (cycles between envelope focus modes)
  + Added action: HeDa_TI_Tracks_Filter with Items.lua
  + Added action: HeDa_TI_Tracks_Filter with Signal.lua
  + Rec Toolbar Button menu: Show Red Border
  + Rec Toolbar Button menu: Show Rec Button only on Selected Tracks
  + Show id Toolbar Button menu: Use color background
  + Show id Toolbar Button menu: Show Color Block when not showing id
  + Show id Toolbar Button menu: Color Block Size
  # Duplicate track: move tracks down after duplication if multiple tracks duplicated
  # Fix solo button on archived tracks
  # Do not show arm button on archived tracks
  # Do not show envelopes button on archived tracks

* v3.11.1 (2026-07-04)
  + Right click isolate button: to isolate any selected tracks instead of pinned tracks.
  # fix panel height when pinned sets are larger than panel
  # fix buttons images

* v3.11.0 (2026-06-29)
  + Proj button: Option to Save Settings per Project. (If enabled, all settings are saved 
  in project. Can toggle on/off to swtich from global to per project settings at any time)
  needs TI 2.4.43
  + Maximize tracks menu: Set Envelopes Visible when needed. (If Maximize focus is on 
  envelopes and envelopes are hidden and active, make them visible when maximizing the track)
  + Maximize tracks menu: Isolating Tracks. (It hides all other tracks when maximizing. Useful when filter 
  is on "ALL", not on "TCP", to be able to switch to other tracks)
  # fix: fix in toggle all tracks TCP envelopes
  # fix: bug performance in minimap scrolling while dragging
  # fix: some optimizations

* v3.10.1 (2026-06-17)
  + Maximize tracks menu: Do not Maximize Tracks with Locked Height
  + Envelope Toolbar Button Menu: Only Volume 
  + Envelope Toolbar Button Menu: Only Pre-FX Volume 
  + Envelope Toolbar Button Menu: Only Pan
  # disable/restore tags auto-visibility when changing TCP/MCP visibility
  # Save/Restore track heights after unisolating pinned tracks
  # fix: different method to open Envelopes window 
  + Added action: HeDa_TI_Tracks_Enable Maximize.lua
  + Added action: HeDa_TI_Tracks_Disable Maximize.lua

* v3.10.0 (2026-06-14)
  + Maximize Button: maximize vertical zoom of the selected tracks
  + Maximize Focus Buttons: Focus to Track
  + Maximize Focus Envelope Buttons: Focus to Track and One Envelope
  + Maximize Focus Envelope Buttons: Focus to One Envelope 
  + Maximize Focus Envelope Buttons: Focus to All Envelopes
  + Envelope Buttons
  + Envelope Buttons Left click: open envelopes window
  + Envelope Buttons Right click: Set Automation mode for selected tracks
  + Envelope Toolbar Button Menu: Toggle Button also Toggles TCP Envelopes on All Tracks
  + Envelope Toolbar Button Menu: Show Number of Envelopes
  + Display settings/Icons Size (30 pixels default)
  # fix: improve filled option in depth lines
  # fix: do not scroll in MCP if track is already visible in mixer
  + Added action: HeDa_TI_Tracks_Focus One Envelope.lua
  + Added action: HeDa_TI_Tracks_Focus All Envelopes.lua
  + Added action: HeDa_TI_Tracks_Focus Track and Envelope.lua
  + Added action: HeDa_TI_Tracks_Focus Track.lua
  + Added action: HeDa_TI_Tracks_Toggle Maximize.lua
  + Added action: HeDa_TI_Tracks_Toggle Quick Solo.lua

* v3.9.2 (2026-05-21)
  # fix a few crashes
  # fix clickable track icon when there is no icon
  # fix track icon border color
  # some signal fixes, tweaks, optimizations
  + track name icon "n" changes to "a" if alternative names are on.
  + Meters menu: Draw Signals on Tracks

* v3.9.1 (2026-05-12)
  # fix recent crash when deleting tracks
  # fix some improvements when signal filter enabled

* v3.9.0 (2026-05-10)
  + Signal filter. Show tracks with some audio signal.
  + Signal button menu: Hold time
  + Signal button menu: Show lost signal indicator
  + Support different configuration for each TI instance
  + QS button (Quick Solo mode): does exclusive solo on mouse hover the tracks. to disable you can click Qs button again, 
  or on any track. To disable QS mode and keep solo on the track, click on the solo button of the track.
  + Ctrl+Alt + Solo buttons: Exclusive Solo
  + Track name button menu: Always show track name on tracks without parent.
  + Track name button menu: Always show on archived tracks.
  + Track name button menu: Always show on selected tracks.
  + Track name button menu: Always show on track under mouse.
  + Track name button menu: Use Alternative Names
  + Track name button menu: Get from Parents Name if Alternative is Not Set
  + Track name button menu: Including Virtual Instrument Suffix
  + Meters menu: Use Track's color toggle
  + Display Settings: Color: Track Name...
  + Track right click menu: Select > Select Only children
  + Individual sizes for track list and pinned track's list
  + Tracks Font Size button. (Shift key) + (Left or right button) to change Pinned tracks size
  + Track Icon button menu: Force square icon
  # fix: improvements when dragging mute solo buttons
  # fix: improvements in minimap zoom, hover
  # improve icons

* v3.8.2 (2026-04-27)
  + Show Track Name button
  + Track Name button right click menu: Always show folders names
  # fix crash in menu
  # some improvements in mini map

* v3.8.1 (2026-04-24)
  + Minimap menu: Show All Tracks Meters Signal (little dot to indicate some metering on track)
  + Display Settings: Do Not Collapse Panel (to avoid accidental collapse clicking 
  on the panel header)
  # fix: TCP pinning issue when using pin to new set
  # fix: updates in rec buttons 
  # Meters: draw vu meter line and hold peak lines on top
  + Toolbar Routing Lines Menu: filled color option
  + Toolbar Routing Lines Menu: filled color opacity option
  + Toolbar MuteSolo button: toggle always show mute and solo buttons, not only on mouse hover.
  + Toolbar MuteSolo button: indicate any solo, and unsolo all tracks when clicked.
  + Toolbar MuteSolo button right click menu: Toggle Dim not soloed tracks.
  + Toolbar MuteSolo button right click menu: Dim Intensity (from 0.0 to 1.0 )
  # various pixel adjustments fixes, search box, lines on spacers...
  # fixes in main menu
  # improved rec arm button images
  # fix crash on projects that have missing track icons 
  # changed some default settings

* v3.7.1 (2026-02-23)
  + Meters: Meters button to show/hide meters. Right click for meter options.
  + Meters options: Full meter width
  + Meters options: Meter height
  + Meters options: Meter colors
  + Meters options: Meter peaks
  + Meters options: Meter hold peaks
  + Meters options: Meter LUFS-S line
  + Meters options: Show meter from channels 1/2 only
  + Meters options: Meters min dB value

* v3.6.1 (2026-02-01)
  + search history (searchbox: enter to save preset, "..." button to access presets menus. Alt click to delete preset )
  # search moved to filter toolbar
  + Rec-arm toolbar button menu: Alt+click shows recording mode menu. otherwise just toggles between overdub mode and normal.
  + Main buttons: Show Toolbar button (Displays button to toggle the display of Track List toolbar buttons)
  + Main buttons: Show armed area button  (Displays button to toggle the displayof armed tracks at the top)
  + Rec-arm toolbar button: toggles display of rec arm button on each track.
  + Rec-arm toolbar button (Right click): menu with options for rec arm button
  + Rec-arm buttons on tracks: Left click: exclusive arm 
  + Rec-arm buttons on tracks: Right click: toggle arm 
  + Rec-arm buttons on tracks: Alt click: toggle Overdub mode
  + new rec button icon for overdub mode.
  + Automatic pinning of armed tracks option
  + Darken not soloed tracks
  # fix various track pinning sets bugs
  # change muted button color to match the default theme and default color text for muted tracks
  # number of items in track before tags so tags are aligned the same in tracks with items.
  # FX buttons: tracks with instruments renamed to VI. configurable name for FX andVI.
  # pixel adjustments in track icons and fx buttons
  # bug fixes and improvements

* v3.5.2 (2025-11-21)
  # fix CTRL click to select multiple broken in 3.5.1
  # fix some crashes
  # improve double clicking
  # fix CTRL+double click to isolate folder sometimes not working the first time.

* v3.5.1 (2025-11-18)
  + Track Icons support
  + Right click Show track icons button: show track icons menu
  + Track icons menu: Show empty track icons
  + Track icons menu: Show border
  + Mini map menu: Enable Zoom on Hover
  + Mini map menu: Set Zoom Factor on Hover...
  + Right click show mini map button: Show mini map menu too
  + Filter archived tracks button on/off
  + Filter archived tracks (right click show only archived tracks)
  + Search filter by tags entering #tag in search box
  + >> Button: Apply filtered visible tracks to TCP/MCP visibility
  + >> Button: Right click to auto apply
  # highlight fx button text if fx chain or instrument is visible
  # improvements on tracks scrolling and TI panels scrolling/repositioning on track change
  # fix middle mouse button drag to scroll TI
  # fix map mouse wheel scroll speed using normal scroll speed
  # changed some defaults settings
  # removing alt+click on track to remove it until I figure out why it crashes sometimes
  
* v3.4.1 (2025-10-15)
  + Double Click Folder: Collapse/Uncollapse folders
  + Display Settings: Text Color for Selected Tracks
  + Display Settings: Border Color for Selected Tracks
  + Display Settings: Background Color for Selected Tracks
  + Show routing line to master track for top level tracks
  # Changed Scrolling internal system (again)
  # when enabling "Depth level Mininum" filter, align track to the left, not wasting space
  # fix in Always show selected track (multiple tracks)
  # change "Always show selected track" default to enabled
  # "Open/Close folder" renamed to "Isolate/Unisolate folder"
  # adjustment of identation in navigator map
  # fixes in minimap navigator scroll indicator
  # more small bug killing... no real bugs were harmed

* v3.3.2 (2025-09-30)
  + ALT+click on track to remove tracks.
  # TI back button scrolls into view.
  # pixel adjustments in fx buttons.

* v3.3.1 (2025-09-19)
  + Show Lines. displays tree parent routing lines.
  + Click line to open track's routing window
  + Enter/exit folder in track right click menu
  + Ctrl+Double click track folder to enter/exit folder
  + < button to close folder
  # changed mouse hover Version buttons appearance
  # fix in scrolling on right drag to select tracks
  # fix crash at start if all tracks hidden
  # do track pinning using 7.46 api functions.
  # bug fixes
  # optimize filtering

* v3.2.6 (2025-09-12)
  # fix fixes on a better day
  # optimization while dragging mini map

* v3.2.5 (2025-09-11)
  # fix recentering again. needs TI v2.4.23

* v3.2.4 (2025-09-10)
  # small bug fix when there are no tracks and trying to scroll.
  # improvments in archiving/unarchiving for archiving v0.5
  + display Add-on version in panel menu. opens changelog txt.
  # remove scroll to the middle of track list when track clicked in the track list.

* v3.2.3 (2025-08-19)
  # fix color issues on Windows OS.
  # fix top margin before toolbar

* v3.2.2 (2025-08-18)
  # fix crash closing project sometimes
  # fix crash setting custom buttons when there are not presets settings

* v3.2.1 (2025-08-17)
  # fix crash if start in empty project

* v3.2.0 (2025-08-17)
  + TI Custom Buttons support. Add custom buttons as in any other panel.
  + Depth Filter Minimum
  + Depth filter right click menu: Always Show Selected Track
  + Depth filter right click menu: Always Show Selected Track Parents
  + Depth filter right click menu: Hightlight Parents border
  + Depth filter right click menu: Show Parents Depth number
  + Depth filter right click menu: Show Depth number on all tracks
  + Show Tracks list button. Toggleon/off the list of tracks. 
  + Panel Menu: Buttons: "Show Tracks button"
  + Panel menu: "Custom Buttons at the Top" to toggle position
  + Panel menu: "Add Custom Button..."
  + Panel Menu: Display Settings: "Text Color for Hidden Tracks..."
  + Panel Menu: Display Settings: "Text Color for Muted Tracks..."
  + Panel Menu: Display Settings: "Left Area Background Opacity..."
  + Panel Menu: Display Settings: "Color: Pinned Area Background..."
  + Panel Menu: Display Settings: "Color: Toolbar Background..."
  + Panel Menu: Buttons: "Show ALL button"
  + Panel Menu: Buttons: "Show TCP button"
  + Panel Menu: Buttons: "Show MCP button"
  + Panel Menu: Buttons: "Show ITEMS button"
  + Map Right click menu: "Map Background Color..."
  + Depth number indicator. 
  + Panel Menu: Buttons: "Show depth button"
  # fix issue opening and closing tracks with versions
  # fix tags size in folder tracks.
  # fix scroll track list selected track into view
  # fix scroll issue when filtered list
  # moved "show map on hover" option to map menu.
  # internal improvements and stability

* v3.1.1 (2025-08-05)
  + Right click depth level filter to set specific number
  + Display Settings: Archived Color...
  + Display Settings: Archived External Color...
  + Display Settings: Archived Global Color...
  # Right dragging tracks, now it selects the tracks while still dragging
  # fix bottom drag end of track list and then right click unselects them.
  # fix crash unisolating pinned tracks in some cases
  # fix filter buttons state in some cases
  # removed panel size from settings menu (there is the bottom handle ro resize it)
  # pixel adjustments

* v3.1.0 (2025-08-02)
  + Settings Presets: Save, Update and Delete Presets 
  + Settings Presets: Reset Settings to default.
  + Items button: Display little indicator if track has items 
  + Items button: Right click Menu: Show number of items
  + Track menu: Move Track to Folder...
  + Track menu: Insert Track
  + Track menu: Insert Virtual Instrument Track...
  + Adjust Track list to Meter panel if needed.
  + Double click after last track to insert track at end of track list.
  # Background Track Color Opacity for Folders use track's color
	+ Background Shade Opacity for folders. a gray overlay over background on folders
  + Display Settings: Margin before track
  + Map Menu: Show Scroll Area Position
  # fix isolate pinset on mixer(MCP)
  # on pinned tracks, do not use margins, spacers (for more compact pinned area)
  # fix mouse over track with bottom margin
  # fix glitch with minimap on hover only
  # fix crash when on start and no tracks and no meters panel
  # fix save zoom settings after zoom change
  # fixes in solo/mute buttons
  # fix: bug update last track
  # fix: improve right dragging selection when mouse below last track

* v3.0.6 (2025-07-17)
  + Display settings: Track Bottom Margin. An additional margin atthe bottom of the tracks. 
    The value is relative to the text height. 0.0 to disable
	+ Display settings: Track Bottom Margin for Folders. 
  # Color Size: now entering value of 0.0 to disable. And negative value to stretch until left margin.

* v3.0.5 (2025-07-17)
  # changed color of empty fx button
  # fix refresh addons when clicking a track to change track selection

* v3.0.4 (2025-07-12)
  + Show minimap button
  + Indicate if tag is Autotag with a border in the tag rectangle.
  + Select Children. and grouped select functions in submenu
  + ... shown if track name does not fit
  # Default font changed to Open Sans Condensed
  + Display Setting: Font vertical Margin for Folders (to adjust vertically the text on folders)
  # fix crash search and press enter (broken in 3.0.1)
  # fix CTRL click track not refreshing.
  # fix crash showing tags sometimes
  # fix track name overlap with tags, tag rectangle sizes
  # fix colors in some icon buttons
  # 1px thicker border for selected track

* v3.0.3 (2025-07-08)
  # fix size some buttons
  # fix duplicate track only creating extra duplication
  # fix duplicate tracks respecting autoarm mode
  # fix rec armed tracks background red color on folders
  # fix color of bypassed fx

* v3.0.2 (2025-07-08)
  # fix scroll track into view when selecting from track list (again)

* v3.0.1 (2025-07-08)
  + Pin to New Set: Directly pin to a new set asking for set name
  # Pin to current set: diplay current set name in the menu
  # fix: scrolling TCP and MCP into view when clicking hidden tracks now tries to scroll nearest visible track.
  + Minimap right click menu: Scroll tracks into view while dragging on map

* v3.0.0 (2025-07-07)
  + Redesigned UI, toolbar icons
  + Font and font size settings, for tracks and folder tracks
  + Mute and Solo Buttons 
  + Panel menu setting: show map on hover. if show map is enabled, but only show if mouse is over the map area.
  + Track Versions support
  + Improved Archive/Unarchive support
  + Isolate and Pin 
  + Improved Zoom button Left button click and hold, make font smaller, right button larger.
  + Background track color opacity from 0 to 1.0 (0 to disable)
  + Right click FXi button opens fx chain.
  + Select Connected tracks and Connected Parents 
  + Duplicate Track and Duplicate Track Only(witohut items/envelopes)
  # fixes in pin presets buttons
  # Unarchive buttons also unarchive connected parent tracks if they are archived
  # Better drag to mute and solo consecutive tracks.
  # more pixel perfect adjustments
  # rearranged Tracks panel menu
  # many bug fixes

* v2.1.0 (2025-06-13)
  # various bug fixes. needs TI v2.4.9
  + Solo buttons, click or click and drag to solo tracks.
  + Right click track menu: added Solo and Mute.
  + Depth toggle button. now the "Dn" is a button to toggle the depth filter on/off without changing the 
  depth level filter value.
  + FX button to quickly toggle display all FX buttons
  + Panel Settings: Smooth Scroll. from 0.1 to 0.9 for speed. 0 o disable.
  # ArmRec as image button. click to unarm.
  # fix unarchive button not working if it was clicked and track not selected
  # fix some things in Archive External and Archive Global (when using Archiving add-on)
  # pixel adjustments here and there, cosmetics

* v2.0.0 (2025-05-28)
  + Map mini navigator
  + Improved multi selection
  + Right mouse button Drag: to select multiple tracks while dragging
  + CTRL + Right Drag: to add to selection multiple tracks while dragging
  + Archiving addon support. Archive and unarchive work for external and global archived modes
  + "All" button. To display all tracks including hidden ones
  + Visual Spacers support, separation space and/or line separator
  + Setting: Spacer size: set to a number as track height multiplier (set to 0 to disable spacers)
  + Setting: Show Spacer line: draws a line in tracks with spacer
  + Setting: Show Track line: draws a line in each track
  + Setting: Indent size: size of children indent (set to 0 to disable)
  + FX chain button (Click to float vst instrument or open chain, CTRL+click to open chain, SHIFT+click to bypass chain)
  + Setting: Show FX  button
  + Setting: Show empty FX button (show even if the fx chain is empty)
  + Arm Recording indicator
  + Mute and Solo indicators (as text color)
  + Setting: Show track id number toggle
  + changed the way tags are displayed, and list of tags in menu, and popup on hover
  + Drag bottom edge of panel to resize panel to see more or less tracks
  + Go to Panel Tracks action

* v1.2.1 (2024-08-15)
  # fix glitch when typing in search box.

* v1.2.0 (2024-08-14)
  + In search box, Tab key, or Down arrow Key: switches focus to track list, and selects first track in the filtered list
  + In search box, Enter key: select first track in the filtered list
  + If focus is on track list, Up and Down arrow keys to select tracks from the filtered list
  + If focus is on track list, Tab key to switch focus back to search box
  + panel menu: more options to show/hide individual option buttons

* v1.1.1 (2023-08-26)
  + zoom buttons to change font size (a very small unreadable font size can still be like a map view)
  + Depth level filtering buttons to increase/decrease the folder depth maximum value
  # fix: Tags column alignment to the right
  # fix: some optimizations

* v1.1.0 (2023-08-09)
  + REAPER 7 support

* v1.0.1 (2021-11-22)
  + Do not steal focus for search box when switching pages
  + Focus search box when running the action: HeDa_Track Inspector 2 VIP_Go to panel Tracks

* v1.0.0 (2021-10-08)
  + Search filter (Tab key, or right click to clear search box)
  + Track: Right click to show track menu
  + Panel Menu: Clear and Save sets of favorites
  + Set buttons: left click load set of favorites
  + Set buttons: right click select tracks from set
  + TCP and MCP filter
  + Tags display 
  + Ctrl click to select multiple tracks
  + Shift click to select multiple contiguous tracks, 
  + Panel Menu: show/hide different elements
  + Panel Menu: refresh time to update track list
    